library ieee;
use ieee.std_logic_1164.all;

entity when_else is
	port(
		A: in std_logic;
		B: in std_logic;
		Y: out std_logic
	);
end when_else;


architecture behav of when_else is
begin
	Y <= A and B when B = '1' else
		A or B when B = '0' else
		nor A;
end behav;